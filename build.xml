<?xml version="1.0"?>
<project name="web.lab3" default="build">
    <property file="build.properties"/>

    <!--    mvn dependency:copy-dependencies -DoutputDirectory=lib-->

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>


    <target name="compile" depends="clean">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath" includeAntRuntime="false"/>
    </target>


    <target name="build" depends="compile">
        <copy todir="${build.dir}">
            <fileset dir="${webapp.dir}"/>
        </copy>
        <copy todir="${build.dir.lib}">
            <fileset dir="${lib.dir}"/>
        </copy>
        <copy todir="${classes.dir}">
            <fileset dir="${resources.dir}"/>
        </copy>

        <jar destfile="${build.dir}/${ant.project.name}.jar">
            <fileset dir="${classes.dir}"/>
            <manifest>
                <attribute name="Created-By" value="krevetkot &amp; darb0ga"/>
                <attribute name="Manifest-Version" value="1.0"/>
                <attribute name="Main-Class" value="NoClass"/>
            </manifest>
        </jar>

        <war destfile="${build.dir}/${ant.project.name}.war" webxml="${build.dir}/WEB-INF/web.xml">
            <fileset dir="${build.dir}"/>
            <manifest>
                <attribute name="Created-By" value="krevetkot &amp; darb0ga"/>
                <attribute name="Manifest-Version" value="1.0"/>
                <attribute name="Main-Class" value="NoClass"/>
            </manifest>
            <classes dir="${classes.dir}"/>
        </war>

        <antcall target="music"/>
    </target>

    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="scp">
        <exec executable="${pscp.path}" failonerror="true">
            <arg value="-P"/>
            <arg value="2222"/>
            <arg value="-pw"/>
            <arg value="${scp.password}"/>
            <arg value="${build.dir}/${ant.project.name}.war"/>
            <arg value="${scp.user}@${scp.server}:${scp.dir}"/>
        </exec>
    </target>

    <target name="music">
        <exec executable="powershell" spawn="true">
            <arg value="-c"/>
            <arg value="(New-Object Media.SoundPlayer '${music.file}').PlaySync();"/>
        </exec>
    </target>

    <target name="native2ascii">
        <delete dir="${native2ascii.resources}"/>
        <native2ascii src="${resources.dir}"
                      dest="${native2ascii.resources}"
                      includes="**/*.properties"/>
    </target>

    <target name="alt">
        <delete dir="${alt.dir}"/>
        <property file="${replacements.file}"/>
        <copy todir="${alt.dir}">
            <fileset dir="${src.dir}"/>
        </copy>

        <!--        <apply-replacements target.dir="${alt.dir}"/>-->

        <!-- Генерируем Java-класс для замен -->
        <echo file="${alt.dir}/Replacer.java"><![CDATA[
import java.nio.file.*;
import java.util.*;
import java.io.*;

public class Replacer {
    public static void main(String[] args) throws Exception {
        System.out.println("[DEBUG] Replacement process started");
        // Загрузка замен
        Map<String,String> replaces = new HashMap<>();
        try (InputStream is = new FileInputStream("${replacements.file}")) {
            Properties props = new Properties();
            props.load(is);
            props.forEach((k,v) -> replaces.put((String)k, (String)v));
            System.out.println("[DEBUG] Loaded replacements: " + replaces);
        }

        // Обработка файлов
        Files.walk(Paths.get("./"))
            .filter(p -> p.toString().endsWith(".java") && !p.toString().contains("Replacer.java"))
            .forEach(p -> {
                try {
                    String originalContent = new String(Files.readAllBytes(p));
                    String modifiedContent = originalContent;

                    for (Map.Entry<String,String> e : replaces.entrySet()) {
                        modifiedContent = modifiedContent.replace(e.getKey(), e.getValue());
                    }

                    if (!originalContent.equals(modifiedContent)) {
                        Files.write(p, modifiedContent.getBytes());
                    }
                } catch (Exception e) {
                    System.err.println("[ERROR] In file: " + p);
                    e.printStackTrace();
                }
            });
        System.out.println("[DEBUG] Replacement process completed");
    }
}
]]></echo>

        <javac srcdir="${alt.dir}" includes="Replacer.java"
               includeantruntime="false" debug="true" verbose="true">
            <compilerarg value="-Xlint:all"/>
        </javac>

        <java classname="Replacer" fork="true" dir="${alt.dir}" failonerror="true">
            <classpath path="${alt.dir}"/>
<!--            <redirector outputproperty="replacer.output" alwayslog="true"/>-->
        </java>


    </target>

</project>